describe('Next Devserver Plugin', () => {
  it('should update next webpack config for nx support', () => {});

  it('should allow the user to extend the webpack config', () => {});

  it('should use the next.template.html', () => {});
});
